pipeline {
    agent any
    stages {
        stage('Test') {
            steps {
                echo 'Run unit tests from the source code'
            }
        }
        stage('Deploy') {
            steps {
                echo 'Deploy project to server'
            }
        }
    }
}